# PRD - 완전랜덤 로또 번호 추천 사이트

## 1. 배경
사용자는 동행복권 사이트에서 매주 자동으로 로또 번호 5개 조합을 구매하고 있지만, 사이트의 자동 번호 생성 방식에 대한 신뢰가 낮다.

본 프로젝트는 사용자가 직접 접근할 수 있는 독립적인 웹사이트에서 로또 6/45 번호 조합을 생성해주는 서비스를 만든다. 핵심 가치는 “납득 가능한 무작위성”이다.

## 2. 문제 정의
- 동행복권 자동 선택 번호가 정말 무작위인지 사용자가 검증하기 어렵다.
- 기존 번호 추천 사이트는 통계, 패턴, 예측을 강조하는 경우가 많아 오히려 신뢰가 떨어질 수 있다.
- 사용자는 복잡한 분석보다 간단하고 투명한 랜덤 번호 생성기를 원한다.

## 3. 목표
- 로또 6/45 번호 조합을 5개 자동 생성한다.
- 생성 과정이 최대한 단순하고 투명하게 느껴지도록 설명한다.
- 사용자가 매주 빠르게 사용할 수 있는 모바일 친화적인 웹사이트를 제공한다.

## 4. 비목표
- 당첨 예측, 통계 기반 추천, 번호 조작 가능성 암시 기능은 제공하지 않는다.
- 구매 대행, 동행복권 로그인, 결제, 자동 구매 기능은 제공하지 않는다.
- 당첨 확률을 높인다고 주장하지 않는다.

## 5. 대상 사용자
- 매주 로또를 구매하는 개인 사용자
- 동행복권 자동 번호를 신뢰하지 못하는 사용자
- 복잡한 통계보다 깔끔한 랜덤 추천을 원하는 사용자

## 6. 핵심 가치
1. 완전 랜덤에 가까운 번호 생성
2. 생성 방식의 투명성
3. 빠른 사용성
4. 모바일 최적화
5. 과장 없는 정직한 UX

## 7. MVP 기능

### 7.1 번호 생성
- 기본적으로 5개 조합을 생성한다.
- 각 조합은 1~45 사이의 중복 없는 숫자 6개로 구성된다.
- 각 조합 내 숫자는 오름차순으로 표시한다.
- “다시 생성” 버튼으로 새 조합을 만들 수 있다.

### 7.2 랜덤 방식
- 브라우저의 `crypto.getRandomValues()` 기반 난수 생성을 우선 사용한다.
- 지원하지 않는 환경에서는 명확히 안내하거나 fallback을 사용한다.
- 화면에 “브라우저 보안 난수 기반”이라는 설명을 제공한다.

### 7.3 복사 기능
- 생성된 5개 조합을 한 번에 클립보드에 복사한다.
- 예시 형식:
  - 1게임: 3, 11, 18, 24, 32, 41
  - 2게임: 6, 9, 13, 21, 37, 45

### 7.4 공유 기능
- 가능하면 Web Share API를 사용해 공유한다.
- 미지원 브라우저에서는 복사 기능으로 대체한다.

### 7.5 안내 문구
- “이 서비스는 당첨을 예측하지 않습니다.”
- “모든 번호 조합의 당첨 확률은 동일합니다.”
- “재미와 개인 선택 보조용으로만 사용하세요.”

## 8. 향후 기능 후보
- 생성 기록 로컬 저장
- 마음에 드는 조합 고정
- 제외할 숫자 설정
- 포함할 숫자 설정
- QR/이미지 저장
- PWA 설치 지원
- 주간 알림
- 서버 기반 공개 시드 랜덤 생성
- 생성 알고리즘 설명 페이지

## 9. UX 초안

### 첫 화면
- 타이틀: “완전랜덤 로또 번호 생성기”
- 설명: “브라우저 보안 난수로 5게임을 바로 생성합니다.”
- 번호 카드 5개
- 버튼:
  - 다시 생성
  - 복사하기
  - 공유하기

### 신뢰 설명 영역
- `crypto.getRandomValues()`를 사용한다는 짧은 설명
- 통계/예측이 아니라 순수 랜덤이라는 설명
- 당첨 확률 관련 주의 문구

## 10. 성공 기준
- 사용자가 접속 후 3초 안에 5개 조합을 확인할 수 있다.
- 모바일에서 버튼과 번호가 명확히 보인다.
- 복사 기능이 정상 작동한다.
- 코드상 난수 생성 방식이 명확하게 확인 가능하다.
- 과장 광고나 당첨 가능성 오해를 유발하는 문구가 없다.

## 11. 권장 기술 스택
MVP 기준:
- Vite + React + TypeScript
- 정적 배포: GitHub Pages / Vercel / Netlify / Cloudflare Pages
- 서버 없음

### 구현 결정 메모
- 제품명: `완전랜덤 로또 번호 생성기`
- 디자인 톤: 신뢰감 있는 유틸리티
- 앱 구조: 단일 페이지, 히어로 영역 + 5게임 카드 + 신뢰/주의 정보 영역

대안:
- Next.js: SEO/확장 페이지가 필요할 때
- 순수 HTML/CSS/JS: 극도로 단순한 버전이 필요할 때

## 12. 주요 리스크
- “완전랜덤”이라는 표현이 수학적/암호학적으로 과장될 수 있음
  - 대응: “브라우저 보안 난수 기반”으로 설명 보완
- 사용자가 당첨 확률 상승으로 오해할 수 있음
  - 대응: 명확한 고지 문구 표시
- 클립보드/공유 API 브라우저 호환성
  - 대응: graceful fallback 제공
- 보안 난수 미지원 브라우저 존재
  - 대응: fallback 사용 사실과 권장 브라우저 안내 문구 표시
